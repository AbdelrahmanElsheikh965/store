import { ProductIntPricePipe } from './product-int-price.pipe';

describe('ProductIntPricePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductIntPricePipe();
    expect(pipe).toBeTruthy();
  });
});
